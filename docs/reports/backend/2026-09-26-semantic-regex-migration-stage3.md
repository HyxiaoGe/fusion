# 语义正则迁移第三阶段：真实 API 复验补修

## 触发证据

- 第一、二阶段已完成 PR、CI 和 dev 部署；真实 API 验收分别发现禁网天气回答补充无来源气候数值、正确车次比较被残留票价词面规则误拦、天气跨日期汇总触发逐值校验误拦，以及地点空格短语被改写为高德 OR 查询。原始 run 和结果详见[阶段一报告](2026-09-26-semantic-regex-migration-stage1.md)与[阶段二报告](2026-09-26-semantic-regex-migration-stage2.md)。

## 本次改动

- 沿用现有无工具边界提示词，要求无相关工具或用户来源时不补充典型天气、数值范围等具体事实；天气产品回答聚焦用户询问日期。两项是模型行为约束，静态检查不能证明实际回答正确。
- 地点搜索保留空格短语，只将逗号、顿号等显式备选分隔符转换成高德 OR；工具说明要求模型保留用户指定的行政区。
- 移除把所有“票价”字样当作无依据实时断言的词面拦截，保留与返回候选直接核对的金额、最低价及明确价格比较校验；预算金额只在对应用户原文和相邻预算标签支持时豁免。对“但、且、并、括号”等分开的正面断言和“不含余票”限制语分别核对，避免限制语遮住无依据准点率。
- 独立代码复审提出的缺价候选、无单位与人民币符号错误金额、错误最低价、预算金额冒充票价、同句限制语遮盖等反例，均先写失败回归再修复。已有结构化事实兜底与其正向用例继续保留。

## 本地验证、dev 发布与真实 API

- 本地架构、Ruff、格式及差异检查通过；按 CI 收集入口完整运行 3250 项 unittest 通过、2 项跳过，额外 pytest 清单 241 项通过。独立代码复审发现的回归均已补失败先行用例并复测；静态复审与本地门禁不等于 dev 真实 API 结论。
- PR [#150](https://github.com/HyxiaoGe/fusion/pull/150) 必需检查全绿，合并提交 `cb76030be76018057c302c9880fe238ba449758b`；dev 工作流 [36213168754](https://github.com/HyxiaoGe/fusion/actions/runs/36213168754) 成功。API/UI 发布台账 `current_sha` 均等于该提交；运行 API、knowledge worker 的 image ID `sha256:b2a0616f5ca69d95b1c2c7ae9e63ef115cee407f5b090ec34653e19d2feef03a` 与台账一致。UI 与 adapter 运行镜像也各自匹配台账，各容器重启数为 0，worker 和 adapter 健康。UI 代码未改，UI image ID 与第二阶段相同。
- 同一 dev 探针账号经真实 `/api/chat/send` 对四条旧问题原句复验，均收到 HTTP 200、SSE `done` 且无错误块。禁网天气 run `e51a84d2cfaa430eb8c175cf03f3a105` 完成，外部工具公告和调用均为空，最终没有给出无来源气候数值；这条问题在本样本中消除。
- 明确城市天气 run `6cc33d5379254ef19db41e97620d4e7a` 的 `weather_forecast` 成功，模型末轮聚焦 9 月 27 日并正确声明上午粒度无法确认，但校验器把“按行政区”和“上午是否下雨”的说明误判为天气事实；模型还补充了工具未证明的“没有雷、雪或大风”和未请求的温差。最终再次使用多日事实兜底，仍未回答避雨条件。
- 单类高铁 run `40a7b5da38fc4349826a0fe807f6e227` 的 `search_trains` 成功。模型末轮给出返回中最低价 G809（553 元）和最快 G1（4 小时 54 分），但站名提取把“到达站均为上海虹桥站”误作另一个站名；后置“略快”还让前面的 576 元进入差值数值作用域。最终使用结构化事实兜底。新补丁在本地以这一条持久化模型候选与结果块重放已通过校验，但尚未部署复验。
- 地点 run `0d7495f6fd38499ab88fd6425c2683a8` 为 `incomplete`；空格短语已原样传至高德，`南山 韩式烤肉 烤肉` 查到目标区域的烤肉候选。不过另一次“台球厅 桌球”仍是全市查询，返回其他区，具名烤肉店附近查询失败；没有可核对的步行距离。最终说明无法确认距离，却又推测学府路商圈桌球店密度和动线，这些推测没有工具依据。后续补丁收紧模型的分区查询与地点推断说明，仍需真实复验。

## 尚未完成

- 天气、高铁和地点的上述缺口进入下一次补修；不能用本轮禁网样本通过概括所有回答质量。用户现有 Chrome 登录标签尚不可绑定，页面验收待其恢复可用后执行。此前模型原始提示词探针只说明提示词有可能产生合适答复，不代表 Fusion API 已通过。
