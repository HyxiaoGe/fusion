# 语义正则迁移第六阶段：天气疑问范围补修

## 触发证据

- 第五阶段 PR #152 的 API 镜像在 dev 与发布台账核对一致。原句天气 run `c87c9b82f6f5465b998c671ce82cdf1f` 中，`weather_forecast` 成功，模型候选写出“因此无法确认上午是否下雨”；最终仍因 `weather_fact_mismatch` 退回多日事实兜底，未直接回答用户上午避雨诉求。
- 逐句核对显示温度、风力和“白天多云”均与结构化返回一致。误拦发生在“下雨”：旧校验器因同一分句中更早的“无法确认”而拒绝豁免后面的“是否下雨”。

## 修复与验证

- 只检查距离天气词最近的“是否／能否／会不会／能不能”之后、天气词之前是否已有免责；疑问词之前的限制说明不再影响该疑问。疑问后跟“但会下雨”的肯定断言仍进入事实核对并拒绝。
- 新增真实句式和相反断言回归；天气校验器 73 项通过。第五阶段真实模型候选及其持久化结构化天气结果在本地新代码重放，`is_valid=True, reason_code=ok`。独立只读复审未发现本轮新增的具体错误放行或误拦。
- 完整 3254 项 unittest 通过、2 项跳过；额外 pytest 清单 241 项通过；架构、本次改动文件 Ruff／格式和差异检查通过。PR [#153](https://github.com/HyxiaoGe/fusion/pull/153) 必需门禁通过，合并为 `f20120452cbb01144b1ba5ad3e454330489a1978`。API CI 首次完整测试中的一条轨迹超时用例因 `recorder_timeout` 失败，同一 run 的失败作业重跑通过；该用例并非本轮改动文件。
- dev 工作流 [36217146224](https://github.com/HyxiaoGe/fusion/actions/runs/36217146224) 成功。2026-09-26 12:28（Asia/Shanghai）只读核对 API/UI 发布台账均为上述 SHA，运行 API、worker、adapter、UI image ID 均与各自台账一致，容器运行中。
- 同句真实 API run `9412c969b5844686a3ee00a5d0b20d5e`、conversation `22f4f636-3e27-4308-b6ac-5be5f3ba391c`：`weather_forecast` 成功，模型候选正确指出上午降雨无法确认，但最终仍因 `weather_fact_mismatch` 退回多日预报兜底。新触发语为“无法单独确认上午是否下雨，所以‘上午不下雨’这一点目前无法确认”。连续三轮针对天气措辞补规则仍未收敛，因此第七阶段改由模型负责天气条件语义，代码仅校验可直接核对的字段。
- 原有 Chrome 登录页面尚无可绑定的 Fusion 标签，页面验收未完成；真实 API 天气回答未通过。
