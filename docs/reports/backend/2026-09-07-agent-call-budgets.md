# Agent 调用预算放宽记录

## 交付状态

2026-09-07（Asia/Shanghai），用户明确要求放宽调用预算，Firecrawl 额度充足。已在线调整四项联网预算；Agent 总预算、MCP 默认值、Redis 有效期和续跑预算的代码修改已验证，尚未推送、PR、合并或部署。当前运行代码仍为 `f7880b24`。

| 限制 | 原线上值 | 调整值 | 状态 |
| --- | --- | --- | --- |
| 普通搜索计划 | 2 次 | 40 次 | dev 配置已激活 |
| 研究搜索计划 | 3 次 | 40 次 | dev 配置已激活 |
| 搜索硬上限 | 4 次 | 40 次 | dev 配置已激活 |
| 网页读取 | 5 次 | 100 次 | dev 配置已激活 |
| Agent 模型轮次 | 8 轮 | 64 轮 | 代码已验证，未部署 |
| Agent 工具总次数 | 20 次 | 200 次 | 代码已验证，未部署 |
| Agent 总运行时限 | 300 秒 | 1800 秒 | 代码已验证，未部署；轮次边界检查，非精确墙钟硬截止 |
| MCP 每服务每 Run | 8 次 | 64 次 | 代码和 dev 部署变量已准备，当前容器仍为 8 |

因此当前线上单个 Run 仍受 8 轮、20 次总调用、300 秒限制；不得把联网预算单独提高表述为整体执行限制已完全解除。

## 实际配置变更

- 16:16 核对生效来源为数据库 `agent_strategy/default@2026-07-02.v1`，不是代码默认值。
- 16:24:34 校验四字段调整，现有治理服务返回 valid，无校验问题。
- 16:25:40 使用 `create_runtime_config_entry` 创建并通过 `activate_runtime_config_entry` 激活 `2026-09-07.agent-budget-v2`，ID `c923bc59-0dbe-471a-9428-f027a4f57e62`。仅修改 `network.max_search_calls`、`default_planned_search_calls`、`deep_research_planned_search_calls`、`max_url_read_calls`，其余生效策略原样复制。
- 旧版本 `2026-07-02.v1`、ID `1160578a-1245-558f-9b0a-b90c1cbff022` 保留，必要时通过同一治理服务重新激活即可回退；没有删除配置或历史数据。
- 运行进程的配置缓存最长 60 秒。16:26:56 后在实际部署容器读取到新版本，真实 `NetworkToolBudget.prepare_*` 代码放行 40 次搜索和 100 次读取；这是预算路径验证，外部提供方调用次数为 0，没有发送测试聊天。
- GitHub 仓库/dev 原先都没有显式 MCP 次数变量。已设置并回读 `dev` 环境的 `MCP_MAX_TOOL_CALLS_PER_SERVER_PER_RUN=64`，供下一次正常发布覆盖历史环境中的 8 次；没有因此触发部署。

## 代码改动

- `Settings` 新增可配置的 `AGENT_MAX_STEPS`、`AGENT_MAX_TOOL_CALLS`、`AGENT_TOTAL_TIMEOUT`，默认 64/200/1800，runner 实际装配读取；正常发布与失败回滚工作流、脚本和容器环境透传同步。
- 网络策略代码默认值与缺字段兜底同步为 40/40/40/100，保留显式低预算覆盖。MCP 默认与部署脚本默认同步为 64。
- Redis 锁和流 meta 不随每个 chunk 续期，原固定 600 秒会提前终止长 Run。现在有效期至少覆盖 Agent 总时限加 120 秒收尾窗口，默认 1920 秒；停止、终态清理和旧数据读取不变。
- 续跑创建新的 Run 时采用当前预算，避免继续继承旧 8/20/300；原 Run 的配置快照、消息、计划模式与任务策略保持原记录。

## 验证

1. 新行为回归先红：旧默认在第 3 次普通搜索、第 4 次研究搜索、第 6 次读取，以及旧 8 轮/20 工具/300 秒和 MCP 8 次边界阻断；旧 continuation 也继续继承旧预算。
2. 修改后后端目标集 `218 passed + 67 subtests passed`：覆盖 Agent 默认装配、网络预算、策略、续跑、stream handler、Redis Stream、MCP、运行时配置治理、chat continue 和可靠停止/触顶收尾。
3. 网络测试实际执行参数准备到 40/100 次并验证下一次仍触顶，覆盖普通/研究模式、完整默认与缺字段兜底；原低预算边界测试保留原断言，改用显式低预算 fixture。
4. 10 个改动 Python 文件 Ruff 通过、架构检查通过、两处部署脚本 bash 语法检查及 `git diff --check` 通过。部署脚本独立审查后同步两项正文 SHA-256 fixture，工作流/部署契约测试 67 项通过。
5. 两次独立只读审查（执行链及部署透传）未发现可达 P0/P1。既有 Pydantic/SQLAlchemy 废弃提示和架构缺测试文件警告如实保留。

## 边界

本次没有增加新 MCP。工具失败后的通用替代选择、上游错误保留和替代证据完成判定仍待实现，预算调整不等于恢复机制已交付。单次工具超时、研究修复次数、重复搜索策略、来源推荐和供应商特定配额仍存在；没有声称所有中断均已解除。没有启动本地服务，未进行代码发布后的真实长对话验收。

## 发布前完整回归补充

PR #52 首次 CI 在 3012 项后端测试中发现 4 个仍引用旧预算的测试断言/触顶输入：进度事件总预算、MCP 部署默认值、两处工具执行器搜索触顶。已同步至新预算，保留触顶不调用 handler、事件先于日志等原行为断言。补修后本地完整后端 `3012 tests OK (skipped=2)`，耗时 51.709 秒；目标 Ruff 与差异检查通过。首次失败未合并或部署。
