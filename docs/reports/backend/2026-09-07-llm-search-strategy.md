# 搜索策略由模型控制：实现与验收

## 原始证据

真实会话 `97e8182e-74d3-4b0b-8f3f-925eb617e20b`，Run `7e3369c3f104498096b33b75d25b650b`，问题“英伟达投资Perplexity意味着什么？”。两次搜索实际分别请求3/5条，返回2/5条，读一篇转载后结束；正文出现无法解析的 `[U1]`。对比用户既有页面：豆包4关键词18资料、DeepSeek11网页、千问17搜索来源、ChatGPT19来源（各平台展示口径不同），原始报道覆盖更广。

根因包括：工具未暴露count；服务端意图预算覆写3/5条；提示词限制普通问题最多两次、同语言且强制年份；未传recency时客户端默认最近一周；url_read未接收运行级编号并硬编码U1。

## 四项实现

1. 多角度、多批次：提示词要求按事件原始报道、业务关系、独立分析等证据需求选择查询角度，同轮多个web_search复用现有并行执行器，后续按缺口继续。移除普通最多两次与语言/年份限制，不强制每个简单问题搜多次。
2. 结果规模与筛选：新增模型count参数，默认10，范围1–20，保留URL去重和同域多样性处理；全局来源注册表复用跨批编号。上下文摘要仍受治理配置上限控制，超出的来源只展示编号、标题和URL，允许模型选择继续读取，不作为已读证据。
3. 证据与引用：提示词要求核对原始来源、日期、独立性及矛盾，缺口存在则继续搜/读。url_read接收运行级编号，搜索后读同URL复用编号，新URL递增；失败不伪造编号。候选元数据不能通过失败恢复证据门禁，成功读页后可登记。历史U1无法唯一映射，未猜测重写。
4. LLM控制：模型决定关键词、批次、count、语言、时效和是否继续；服务端保留总搜索40次、读页100次和运行总预算，按完整请求签名去重。旧DB里的意图/追问/repair小数量、旧计划次数不再参与执行，无需数据库迁移。未指定recency时传空时间过滤，保留历史原始报道。

## 验证

- 搜索schema与客户端先红后绿，验证count暴露和无默认一周窗口。
- 实际runner→tool_round→executor→handler的隔离集成用事件屏障证明同轮2/3/4搜索并行，独立count10–13及后续16/18到达provider；普通问题可进行第三批检索和后续读页，tool_call_id与引用不混淆。
- 旧DB配置、同关键词不同domain/recency/count、相似词互补搜索、完全相同请求去重、全局预算耗尽均有回归。
- 同页复用编号、新页编号与持久化source_refs一致；metadata不能当作读过正文的证据。独立审查发现此P1后已修复并验证，无剩余可达P0/P1。
- 前端现有引用模型和来源面板4文件64项Vitest通过。改动Python Ruff与git diff检查通过。
- 最终完整后端unittest：3049项，50.576秒，OK（skipped=2）。旧数量断言按真实输入更新，耗尽不执行handler及事件顺序断言保持。

隔离集成模拟了LLM和外部搜索/读页，不代表真实模型检索质量验收。首次发布及真实回归如下；读页缺口补修后仍需再次验收。


## 首次发布与真实回归（北京时间2026-09-07）

- PR #54，master `c6f21fdbc30abd2bc7f0562ca8258eeaf9e5e15a`；PR CI `34114101857`、主干CI `34114761599`成功。
- 部署 `34114762175`最终成功，API/UI容器版本与发布账本匹配、运行中、重启0。API digest `sha256:f7745300ba849c9a5e129d442095f702c3f29533f6bf6d4cb1e18748f75b7dd1`，UI digest `sha256:0d9129e58e172aab09eb6267f4de4454094f9c1163c0eda9ed531eff1de90c81`。API健康检查数据库/Redis连接正常。
- UI首次构建有一项既有模型管理异步测试失败（刷新中status与最终上线status竞态），主干同提交测试已绿；仅重跑失败任务后全流程成功，没有跳过测试或改断言。
- 复用Chrome extension既有登录标签643084497，DeepSeek V4 Flash、思考和自动执行保持原设置，直接发送两道原题。
- 投资问题：[会话](https://fusion.seanfield.org/chat/c53c796f-d58e-449c-b8c6-b5c93d768c8f)，Run `990d6b89708f4de88b174c970aab8ad7`，4轮6工具55.525秒。第一轮中英文2个查询，第二轮补查交易是否落地，3次均请求/返回10条（共30条原始命中，非30个独立来源），3次读页；界面去重后已使用7条、候选5条。数字引用能打开正确来源面板，刷新后回答及145条连续完整轨迹保留，无[U1]。
- 香港天气：[会话](https://fusion.seanfield.org/chat/37ca6180-2048-4257-8232-483dfb72e14a)，Run `b554ca4a9cee4afebefa10b7dd357c4d`，4轮5工具38.562秒。高德失败→两个8条搜索→同轮读取天文台网页与开放数据（网页degraded，开放数据成功）；回答涵盖9月11–13日及数字引用17，刷新后答案及100条连续完整轨迹保留。
- 已捕获投资会话/轨迹/标题API为200；网络事件缓冲发生截断，不能声称全程无网络错误。观察到跨域资源ORB/NotSameOrigin阻断及一次匿名脚本startTime异常，尚不能归因为业务代码。API验收时段日志无ERROR/CRITICAL/Traceback。

## 真实回归发现的读页证据缺口及补修

投资读页中Reuters实际仅有CAPTCHA警告和空Markdown正文，却被标成成功；Yahoo实际正文位于约28418字符处，前8000字符为导航。这两项意味着不能把首次的“深读3页”当作3页有效正文，首次投资验收不记作完整通过。

补修限于现有读页链路：标准reader包装后的空正文降级为empty_content/access_blocked，模型得到明确失败与替代来源提示，不新增有效引用或已读证据；原始诊断有界保留在轨迹详情。规范reader包装存在与页面标题精确匹配的一级Markdown标题时，从标题处选取受限正文窗口；无匹配保持原内容，原始工具结果不改写。补修需通过独立审查、测试、CI/CD及同题复跑后闭环。

补修最终本地验证：显式隔离SQLite测试配置、项目Python依赖环境下完整unittest 3058项，49.704秒，OK（skipped=2）；Ruff与diff检查通过，独立审查P1已解除，无剩余P0/P1。此前误用临时依赖环境、未指定测试数据库的两次全量执行存在环境错误，未作为通过证据。
